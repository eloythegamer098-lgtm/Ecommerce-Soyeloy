# ==============================================================================
# NEON STORE - CONFIGURACIÓN DE PRODUCCIÓN
# ==============================================================================

# Entorno
NODE_ENV=production
PORT=3000

# Base de Datos (Ajustar según host de deploy)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=ecommerce_db
DB_PORT=3306

# Seguridad
JWT_SECRET=tu_clave_secreta_super_segura_aqui
ALLOWED_ORIGINS=https://tudominio.com,http://localhost:5173

# Email (SMTP Profesional)
EMAIL_USER=tu_correo@dominio.com
EMAIL_PASS=tu_app_password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465

# Frontend
FRONTEND_URL=https://tudominio.com
VITE_PUBLIC_URL=https://tudominio.com/api/v1
